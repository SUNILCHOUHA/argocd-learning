apiVersion: apps/v1
kind: Deployment

metadata:
  name: apache-deploy 
i
spec:
  replicas: 2
  selector: 
    matchLabels:
      app: apache-po 


  template:
    metadata:
      labels:
        app: apache-po
    spec:
      containers:
        - name: demo
          image: nginx
          ports:
          - containerPort: 80
